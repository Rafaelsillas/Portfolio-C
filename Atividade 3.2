#include <stdio.h>
#include <string.h>
#include <locale.h>
//Rafael Sillas Valente da Costa
// Define uma estrutura chamada carro
typedef struct {
char modelo[50]; // Armazena o modelo do carro (limite de 50 caracteres)
int ano; // Armazena o ano de fabricação
double preco; // Armazena o preço
} Carro;
// Função para modificar o preço de um carro
void modificarPreco(Carro *c, double novoPreco) {
c->preco = novoPreco; // Atribui o novo preço ao carro
}
// Função para mostrar os dados de um carro
void imprimirCarro(Carro *c) {
printf("Modelo: %s\n", c->modelo); // Mostra o modelo do carro
printf("Ano: %d\n", c->ano); // Mosra o ano do carr
printf("Preço: R$ %.2lf\n", c->preco); // Mostra o preço do carro formatado
}
int main() {
setlocale(LC_ALL, "Portuguese"); // Configura para português
Carro carrol; // Cria uma variáveldo tipo Caro chamada carrol
strcpy(carrol.modelo, "XYZ"); // Atribui o modelo "XYZ" ao carro carrol
carrol.ano = 2020; // Atribui o ano 200 a carro carro
carrol.preco = 90000.00; // Atribui o preço 90000.00 ao carro carrol
printf("Dados antes da modificação:\n"); // Mostra um cabeçalho
imprimirCarro(&carrl); // Chama a função para motrar os dados do carro
modificarPreco(&carrol, 115000.00); // Modifica o preço do carro para 115000.00
printf("\nDados após a modificação do preço:\n"); // Mostra um cabeçalho
imprimirCarro(&arrol); // Chama a função para mostrar os dados do carro atualizados
return 0; // Fim do programa
}
