#include <stdio.h>
// Rafael Sillas Valente da Costa

void impares(int n);
int main() {
int num;
printf("Digite um número: ");
scanf("%d", &num);
impares(num);
return 0;
}
void impares(int n) {
for (int i = 1; i <= n; i += 2) {
if (i % 7 != 0) {
printf("%d ", i); }
}
}
